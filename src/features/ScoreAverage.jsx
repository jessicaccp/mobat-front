import { useEffect, useState } from "react";
import api from "../services/api";
import Plot from "react-plotly.js";

export default function ScoreAverage({ numIps }) {
  return (
    <>
      <Plot />
    </>
  );
}
